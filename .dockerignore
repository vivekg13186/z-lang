# Things that don't belong in the build context.
.git
.gitignore
.vscode
.DS_Store

# Build outputs
z
z.exe
*.o
*.obj
build/

# Editor / IDE
*.swp
*~

# Plugin folder isn't needed for the runtime image
plugin/

# Avoid pulling the Dockerfile into the image as data
Dockerfile
.dockerignore
